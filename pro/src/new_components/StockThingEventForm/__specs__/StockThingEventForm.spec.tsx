describe('StockThingEventForm', () => {
  let props

  beforeEach(() => {
    props = {
      today: new Date(),
    }
  })

  it('render StockThingEventForm', () => {})

  it('render disabled field in list', () => {})
})
