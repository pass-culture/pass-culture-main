describe('CollectiveOffers', () => {
  it('should display the spinner when the offerer is loading', () => {})
  it('should display the spinner when the venues are loading', () => {})
  it('should fetch only bookable offers when the WIP_ENABLE_NEW_COLLECTIVE_OFFERS_AND_BOOKINGS_STRUCTURE FF is active', () => {})
})
