name: "3 [on_workflow/PRO] Deploy specific version on the live channel"

on:  
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
      app_version:
        type: string
        required: true
      continue_on_error:
        type: boolean
        required: false
        default: false

defaults:
  run:
    working-directory: pro

jobs:
  publish-to-live-channel:
    environment: 
      name: pro-${{ inputs.environment }}
      deployment: false
    if: ${{ github.event.pull_request.user.login != 'dependabot[bot]' }}
    permissions: 
      contents: read
      id-token: write
    runs-on: ubuntu-22.04
    continue-on-error: ${{ inputs.continue_on_error }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: "OpenID Connect Authentication"
        id: "openid-auth"
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          workload_identity_provider: ${{ vars.WORKLOAD_IDENTITY_PRD }}
          service_account: ${{ vars.PRO_DEPLOY_SERVICE_ACCOUNT }}
      - uses: pass-culture/common-workflows/actions/setup-firebase-tools@e6bcac798f0e97e8071b2e87a5c6a28c9bb538f4
      - name: "Clone version to live channel"
        id: "clone-version"
        env:
          ENV: "${{ inputs.environment }}"
          VERSION: "${{ inputs.app_version }}"
        run: |
          firebase hosting:clone "pc-pro-$ENV:$VERSION" "pc-pro-$ENV:live"
