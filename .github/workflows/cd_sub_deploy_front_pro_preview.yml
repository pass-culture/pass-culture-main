name: 3 [CI] Deploy front PRO preview

on:
  workflow_dispatch:
    inputs:
      cache_bucket_name:
        type: string
        required: true
      firebase_cli_version:
        type: string
        required: false
        default: 15.18.0
      time_to_live:
        type: string
        required: false
        default: "2d"
      custom_pcapi_backend_id:
        type: string
        default: ""
        required: false
  workflow_call:
    inputs:
      cache_bucket_name:
        type: string
        required: true
      firebase_cli_version:
        type: string
        required: false
        default: 15.18.0
      time_to_live:
        type: string
        required: false
        default: "2d"
      custom_pcapi_backend_id:
        type: string
        default: ""
        required: false
    secrets:
      PASSCULTURE_GITHUB_ACTION_APP_PRIVATE_KEY: { required: true }
      PASSCULTURE_GITHUB_ACTION_APP_ID: { required: true }
    outputs:
      pro_url:
        description: "The pro (firebase) preview URL"
        value: ${{ jobs.deploy_on_firebase.outputs.pro_url }}

permissions:
  id-token: write
  contents: read

env:
  ENV: testing

defaults:
  run:
    working-directory: pro

jobs:
  deploy_on_firebase:
    runs-on: ubuntu-22.04
    outputs:
      description: "The pro (firebase) preview URL"
      pro_url: ${{ steps.deploy.outputs.preview_url }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version-file: "pro/package.json"

      - name: Setup firebase
        env:
          FIREBASE_CLI_VERSION: ${{ inputs.firebase_cli_version }}
        run: |
          npm install -g "firebase-tools@$FIREBASE_CLI_VERSION"


      - uses: pass-culture/common-workflows/actions/setup-safe-chain@afae2577022181fccdaead9537235886c461f36a

      - name: "OpenID Connect Authentication"
        id: "openid-auth"
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          export_environment_variables: "true"
          create_credentials_file: "true"
          workload_identity_provider: ${{ vars.WORKLOAD_IDENTITY_PRD }}
          service_account: ${{ vars.PRO_PREVIEW_SERVICE_ACCOUNT }}

      - name: Change backend if a custom backend was required
        env:
          CUSTOM_BACKEND_ID: ${{ inputs.custom_pcapi_backend_id }}
        run: |
          sed -i "s/https:\/\/backend.testing.passculture.team/https:\/\/backend-$CUSTOM_BACKEND_ID.testing.passculture.team/g" src/.env.testing

      - name: Install passdirector
        uses: ./.github/actions/setup-passdirector
        with:
          version: v0.3.0
          github_app_id: ${{ secrets.PASSCULTURE_GITHUB_ACTION_APP_ID}}
          github_app_private_key: ${{ secrets.PASSCULTURE_GITHUB_ACTION_APP_PRIVATE_KEY}}

      - name: "Cache the node_modules"
        id: "yarn-modules-cache"
        uses: pass-culture-github-actions/cache@88f07af200f0bcf2480f9c194e3dbb9488a1902b # v1.0.0
        with:
          compression-method: "gzip"
          bucket: ${{ inputs.cache_bucket_name }}
          path: |
            **/node_modules
          key: v1-yarn-pro-dependency-cache-${{ runner.os }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            v1-yarn-pro-dependency-cache-${{ runner.os }}-${{ hashFiles('**/yarn.lock') }}

      - name: "Install dependencies (3 attempts)"
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 # v4.0.0
        with:
          timeout_minutes: 2
          max_attempts: 3
          command: |
            cd pro
            yarn install --immutable

      - run: yarn build:"$ENV"
        env:
          # By default NodeJS processes are limited to 512MB of memory
          # This is not enough for the build process when compiling sourcemaps
          # Increases the limit so that the build doesnt fail
          NODE_OPTIONS: --max_old_space_size=4096
          VITE_APP_VERSION: ${{ github.sha }}

      - name: Deploy Pro preview
        working-directory: pro
        env:
          REF_NAME: ${{ github.ref_name }}
        id: deploy
        run: |
          URL=$(passdirector firebase deploy-preview --project-id "pc-pro-testing" --target "pc-pro-testing" --channel "${REF_NAME}")
          echo "preview_url=$URL" | tee -a "$GITHUB_OUTPUT"

      - name: "Publish Summary"
        run: |
          {
            echo "### :rocket: Deploy PRO summary"
            echo "| Results                              | Value |"
            echo "| ------------------------------------ | ----- |"
            echo "| [pro] preview url                    |  ${{ steps.deploy.outputs.preview_url }} |"
          } >> "$GITHUB_STEP_SUMMARY"
