name: 3 [CI] Daily Security scan

on:
  schedule:
    # “At 09:12”
    - cron: '12 9 * * *'
      timezone: "Europe/Paris"
  workflow_dispatch:

jobs:
  semgrep_scan:
    name: semgrep/ci
    runs-on: ubuntu-latest
    container:
      image: semgrep/semgrep@sha256:a3d49dc967b8534a6a76628e50c51cbfe33eb7195dc2feab1fdc0f100852c8ef # v1.156.0
    if: (github.actor != 'dependabot[bot]')
    permissions:
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 #v6
        with:
          persist-credentials: false

      - name: Perform Semgrep Analysis
        run: semgrep scan --config auto . --sarif-output=semgrep-results.sarif --exclude-rule=python.django.security.django-no-csrf-token.django-no-csrf-token

      - name: Save SARIF results as artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a #v7.0.1
        with:
          name: semgrep-scan-results
          path: semgrep-results.sarif

      - name: Upload SARIF result to the GitHub Security Dashboard
        uses: github/codeql-action/upload-sarif@1f34ec16430d82636d18716acc7aaa6d843b35a9 #v4
        with:
          sarif_file: semgrep-results.sarif
