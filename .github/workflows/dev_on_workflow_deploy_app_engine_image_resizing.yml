name: "[TEMP] Dummy workflow"

on:
  push:
    branches: [fseguin/dummy-workflow]

jobs:
  dummy:
    runs-on: ubuntu-latest

    steps:
      - name: "echo"
        run: echo "Every way the wind goes"
