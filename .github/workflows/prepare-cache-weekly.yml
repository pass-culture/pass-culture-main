name: Reset cache weekly

on:
  schedule:
    - cron: "0 0 * * 0"

jobs:
  prepare-cache-weekly:
    uses: ./.github/workflows/reusable-update-api-client.yml
    with:
      trigger-only-on-change: false
