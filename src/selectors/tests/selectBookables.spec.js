import { selectBookables } from '../selectBookables'
import state from '../../mocks/redux_state'

describe('selectBookables', () => {
  // TODO
  it('should select the ??', () => {
    expect(selectBookables(state)).toEqual({})
  })
})
