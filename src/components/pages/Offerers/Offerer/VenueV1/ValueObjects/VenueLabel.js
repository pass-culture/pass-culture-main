/*
* @debt complexity "Gaël: file nested too deep in directory structure"
*/

export default class VenueLabel {
  constructor(venueLabel = {}) {
    this.id = venueLabel.id
    this.label = venueLabel.label
  }
}
