environments:
  testing:
    values:
      - chartVersion: 0.26.1
  staging:
    values:
      - chartVersion: 0.26.1
  integration:
    values:
      - chartVersion: 0.26.1
  production:
    values:
      - chartVersion: 0.26.1
  ops:
    values:
      - chartVersion: 0.26.1
  perf:
    values:
      - chartVersion: 0.26.1
---
helmDefaults:
  wait: true

repositories:
  - name: passCulture
    url: europe-west1-docker.pkg.dev/passculture-infra-prod
    oci: true

releases:
  - name: {{ .Environment.Name }}
    namespace: {{ .Environment.Name }}
    chart: passCulture/pass-culture-helm-registry/pcapi
    version: {{ .Values.chartVersion }}
    values:
      -  ./pass-culture-deployment/helm/pcapi/values.{{ .Environment.Name }}.yaml
