query get_banking_info_status($dossierNumber: Int!) {
  dossier(number: $dossierNumber) {
    state
    dateDepot
    dateDerniereModification
    datePassageEnConstruction
    datePassageEnInstruction
    dateTraitement
  }
}
