[metadata]
name = pcapi
version = 0.1.0

[options]
python_requires = >=3.10
package_dir=
    =src

[options.packages.find]
where = src

[coverage:report]
omit =
    tests/*
    venv/*
    alembic/*
    .circle/*
    snippets/*
    static/*
