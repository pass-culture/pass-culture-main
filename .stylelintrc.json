{
  "extends": "stylelint-config-standard",
  "rules": {
    "color-hex-case": "upper",
    "color-hex-length": "long",
    "declaration-colon-newline-after": "always-multi-line",
    "at-rule-no-unknown": [
      true, {
      "ignoreAtRules": ["extend", "if", "else", "elseif", "each"]
      }
    ]
  }
}
